import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export function SettingsBackupRouteScreen() {
  return <UnsupportedPlatformScreen eyebrow="Settings" title="Metadata backup export is unavailable on web" />;
}
