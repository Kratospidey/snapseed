import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export function SettingsOnboardingRouteScreen() {
  return <UnsupportedPlatformScreen eyebrow="Settings" title="Onboarding replay is unavailable on web" />;
}
