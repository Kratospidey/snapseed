import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export function RelinkModalRouteScreen() {
  return <UnsupportedPlatformScreen eyebrow="Relink" title="Relink flow is unavailable on web" />;
}
