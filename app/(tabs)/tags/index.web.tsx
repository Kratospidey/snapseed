import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export default function TagsWebScreen() {
  return <UnsupportedPlatformScreen eyebrow="Tags" title="Tag management is mobile-only right now" />;
}
