import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export default function SettingsWebScreen() {
  return <UnsupportedPlatformScreen eyebrow="Settings" title="Settings are limited on web" />;
}
