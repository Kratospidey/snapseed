import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export default function SearchWebScreen() {
  return <UnsupportedPlatformScreen eyebrow="Search" title="Search is mobile-only right now" />;
}
