import { UnsupportedPlatformScreen } from '@/components/feedback/UnsupportedPlatformScreen';

export default function CaptureDetailWebScreen() {
  return <UnsupportedPlatformScreen eyebrow="Capture" title="Capture detail is mobile-only right now" />;
}
